export enum LoginType {
  Unknown = 0,
  Password = 1,
  SsoLogin = 2,
}

export enum LoginIdType {
  Unknown = 0,
  Email = 1,
  QYWXUserId = 2,
}

export enum UserStatus {
  Active = 1,
  Inactive = 2,
}
